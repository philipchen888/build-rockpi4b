Build bullseye-xfce-kde-gnome sdcard for Radxa rockpi4b:

Input files: ../rootfs-bullseye/linaro-esp.img
             ../rootfs-bullseye/linaro-basicrootfs.img:

Plugin NVME-SSD/sdcard/emmc/USB2-SATA-SSD to PC.
sudo ./mksdcardimg.sh
Eject NVME-SSD/sdcard/emmc/USB2-SATA-SSD from PC.

Plugin NVME-SSD/sdcard/emmc/USB2-SATA-SSD to rockpi4b.
mouse right click on the screen, select terminal emulator.
Login:
Username: linaro
Password: linaro
uname -a
lsb_release -a
sudo ifconfig
sudo nmcli dev wifi connect "SSID" password "PASSWORD"
sudo dpkg-reconfigure tzdata
sudo dpkg-reconfigure locales
date
sudo apt update
sudo apt upgrade
sync
sudo rm /etc/xdg/xfce4/xfconf/xfce-perchannel-xml/xfce4-power-manager.xml
sudo apt install tasksel
sudo tasksel --list-tasks
For xfce4: sudo tasksel install xfce-desktop  ( then sudo apt remove --purge openbox )
For kde:   sudo tasksel install kde-desktop   ( then sudo apt remove --purge openbox )
For gnome: sudo tasksel install gnome-desktop ( then sudo apt remove --purge openbox )
sync
sudo shutdown -h now
power off, power on
Disable power saving and screen saver.
glxinfo | grep OpenGL
date
Check if Debian is running UEFI mode:
ls /sys/firmware/efi

sudo apt install speedtest-cli build-essential libjpeg-dev libpng-dev libpng16-16 pkg-config libx11-dev libjpeg-dev libpng-dev libpng16-16 pkg-config libx11-dev python3-pip python3-setuptools chromium chromium-sandbox ffmpeg mpv git pandoc

speedtest-cli

git clone https://github.com/glmark2/glmark2.git -b 2021.02
cd glmark2
./waf configure --with-flavors=x11-glesv2
./waf 
sudo ./waf install
glmark2-es2

aplay -l
pavucontrol  ( select card 1 hdmi as default )

Chromium -> youtube.com -> search YmDNhi07_Ho

git clone https://github.com/yt-dlp/yt-dlp.git
cd yt-dlp
make
cd ../
~/yt-dlp/yt-dlp -f 'bestvideo[ext=mp4]+bestaudio[ext=m4a]' hyUhEjtlDLA -o avemaria.mp4
mpv avemaria.mp4

sudo rfkill list
hciconfig hci0
hcitool dev
hciconfig -a
